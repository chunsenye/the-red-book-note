## 创建对象

在js中没有类的概念，无法创建类，所以开发人员就发明了一种函数，还用了new语法糖，来创建对象，那就是构造函数。

构造函数使用的是工厂模式，但是工厂模式没有办法知道一个对象的类型，所以才有的构造函数默认。（这里我还是不太理解）

### 构造函数模式和工厂模式的区别

1.没有显示的创建对象
2.没有return语句
3.直接将属性和方法直接赋值给this对象

### 构造函数模式下必须使用new创建对象实例

这里很经常会有面试，说是实现一个new。其实挺简单的。
